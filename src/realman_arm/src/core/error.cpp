#include "core/arm_impl.hpp"
// ArmError is fully inline in core/error.hpp.
// This file exists for consistency — all error-related extensions go here.

namespace rm {
// (currently empty — ArmError is header-only)
} // namespace rm
