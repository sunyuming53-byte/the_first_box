#!/bin/bash
# Usage: ssh-remote <ip>
# SSH into the runtime container on the robot MiniPC.
set -e

if [ $# -lt 1 ]; then
    echo "Usage: ssh-remote <robot-ip>"
    exit 1
fi

exec ssh -p 2022 -o StrictHostKeyChecking=no "root@$1"
